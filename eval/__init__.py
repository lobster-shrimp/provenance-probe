"""Accuracy / regression eval harness for the provenance engine.

Two tiers (see eval/README.md for the full rationale):

  * CONSISTENCY tier (`mock.py` + vocab cases) — serves genuine open-weights
    GGUF token counts through a blind endpoint and runs the full assess
    pipeline. Because the mock and the shipped reference are generated by the
    same BPE+regex code, a pass proves the matcher / scoring / reference are
    internally consistent and have NOT regressed. It is NOT an accuracy claim
    about live commercial endpoints.

  * ACCURACY tier (`bundles/` + scoring golden bundles) — feeds labeled
    `score()` bundles (a real non-adverse recording + synthetic threshold /
    edge cases) straight through the scoring logic. This is where the
    false-positive gate has teeth.

Real named-vendor accuracy (DeepSeek/Moonshot CONFIRMED-CN) lives in the
PRIVATE provenance-observatory, not here — publishing an interpreted
named-vendor verdict from this public repo is a Gate-1 legal exposure.
"""
